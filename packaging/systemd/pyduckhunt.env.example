# Root-owned systemd authorization example. This file is deliberately disarmed.
# Copy it to /etc/pyduckhunt/INSTANCE.env only during a reviewed rollout.
PYDUCKHUNT_TARGET_ARGS="--config /home/mediabot/pyduckhunt-pilot/pyduckhunt.toml --allow-host irc.example.invalid --allow-port 6697 --allow-tls --allow-channel #example"
PYDUCKHUNT_CONFIRM_LIVE="DISARMED"
